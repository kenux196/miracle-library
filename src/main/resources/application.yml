spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/h2dbs/miracle-planner
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: info
    org.spring.framework.web: info
    org.kenux.miraclelibrary: debug
